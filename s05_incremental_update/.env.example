# OpenAI 兼容 API 配置。也可以只在项目根目录放一份 .env（code.py 会向上查找）。
OPENAI_API_KEY=sk-your-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
MODEL_ID=gpt-4o-mini

# 可选（环境变量，非 .env）：
#   SOURCE_ROOT     被分析项目根目录（默认 C:\Desktop\Project\WeKnora）
#   WIKI_WORKSPACE  workspace 位置（默认 ..\workspace；隔离演示时可指到临时目录）
